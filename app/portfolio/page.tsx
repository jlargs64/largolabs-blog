import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Largo Labs → Lab Exhibits',
};

export default function Portfolio() {
  return <h1>Lab Exhibits (Portfolio)</h1>;
}
