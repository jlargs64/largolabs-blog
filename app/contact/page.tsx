import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Largo Labs → Collaboration Station',
};

export default function Contact() {
  return <h1>Collaboration Station (Contact)</h1>;
}
